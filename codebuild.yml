version: 0.2

env:
  variables:
    #APP: sagemaker
    ECR_URL: 624733648801.dkr.ecr.us-east-1.amazonaws.com
    #ECR_REPO_NAME: demo

phases:
  pre_build:
    commands:
      #- source `pwd`/version
      #- COMMIT_ID_SHORT=`echo "${CODEBUILD_RESOLVED_SOURCE_VERSION}" | cut -c1-8`
      #- TAG=`echo "${MAJOR}.${MINOR}.${COMMIT_ID_SHORT}"`
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${ECR_URL}
  build:  
    commands:
      - docker-compose build
      #- docker tag demo:sagemaker-safety-image 624733648801.dkr.ecr.us-east-1.amazonaws.com/demo:sagemaker-safety-image
      - docker tag demo:sagemaker-fnb-image 624733648801.dkr.ecr.us-east-1.amazonaws.com/demo:sagemaker-fnb-image
      #- docker tag ${ECR_REPO_NAME}:latest ${ECR_URL}/${ECR_REPO_NAME}:$latest
  post_build:
    commands:
      #- docker push ${ECR_URL}/demo:sagemaker-safety-image
      - docker push ${ECR_URL}/demo:sagemaker-fnb-image


